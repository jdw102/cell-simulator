package cellsociety.model;

import cellsociety.controller.Controller;
import cellsociety.view.DisplayView;

public class TestController extends Controller {
  public TestController(DisplayView displayView) {
    super(displayView);
  }
}
